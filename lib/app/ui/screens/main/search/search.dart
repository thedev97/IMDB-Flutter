import 'package:flutter/material.dart';

class IMDBSearch extends StatelessWidget {
  const IMDBSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Search')),
    );
  }
}
