import 'package:flutter/material.dart';

class IMDBProfile extends StatelessWidget {
  const IMDBProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Profile')),
    );
  }
}

